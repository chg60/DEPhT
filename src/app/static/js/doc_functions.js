function create_toc() {
  const headings = document.querySelectorAll("#content h1, #content h2,
                                              #content h3, #content h4,
                                              #content h5");
  counters = new Array(headings.length)

  toc_text = "";

  // check that there are headings
  if(headings && headings.length) {
    headings.forEach((heading, i) => {
    });

  }
}
